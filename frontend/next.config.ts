import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "**",
      },
    ],
  },
  // Enable strict mode for catching React issues early
  reactStrictMode: true,
};

export default nextConfig;
